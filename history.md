# history.md — 작업 이력

날짜는 2026-09-24, 시각은 KST입니다. 카메라 보드 작업 이력은 `../esp32_cam/docs/history.md`에 따로 있습니다.

## 1. 선행 작업 (esp32_cam 프로젝트)

- 연결된 보드를 식별했습니다: ESP32-S3 (N16R8), 카메라 OV3660, 핀맵 ESP32S3_EYE.
- 공장 펌웨어를 백업했습니다.
- OV3660 프로브 펌웨어로 인식을 확인했습니다.
- CameraWebServer를 업로드하고 스트리밍을 확인했습니다(192.168.45.138).

## 2. 프로젝트 착수 (~19:00)

- 요청 내용:
  - 사물 인식 결과를 `id : [이름] : "상태"` 로그로 출력
  - 가장 정확한 방법을 오케스트레이션으로 조사
  - 문서 3종(architech / research / dev_plan)과 별도 프로젝트 폴더
- 사용자에게 두 가지를 확인했습니다.
  - 인식 대상: **신호등 + 차량 신호**
  - 위치: `workspace\car_siginal_detector` 별도 폴더
- 사용자가 앱 기능 명세를 추가했습니다 → `app_spec.md`
  - 한국 신호등, 앞차 번호판·브레이크·방향지시등·주행 상태, 주변 상황, 차선, 충돌·끼어들기
  - 모든 안내를 음성으로

## 3. 오케스트레이션 조사

- 조사 에이전트 4개(GitHub, Hugging Face, 논문, Espressif)를 동시에 실행했습니다.
- 명세가 추가된 뒤 2개(한국 특화, ADAS)를 더 실행했습니다.
- 결과는 `research.md`에 정리했습니다.
- 주요 결론:
  - ESP32-S3에서 YOLO11n 640을 돌리면 프레임당 26초 → PC에서 추론
  - 한국 신호등 공개 모델 없음 → 규칙 기반으로 시작하고 AI Hub로 학습 예정
  - 후미등 공개 가중치 없음
  - 번호판은 sauce-Git 사용

## 4. 환경 구성과 측정

- `.venv`(Python 3.14)를 만들고 ultralytics 8.4.161, torch 2.14 CPU, openvino 2026.4, pywin32 312, onnxruntime 1.30, lap을 설치했습니다.
- SAPI 음성을 확인했습니다: **Microsoft Heami Desktop - Korean** 사용 가능.
- PC 조건: NVIDIA GPU 없음(i5-1340P, Iris Xe), **배터리 64%, 균형 모드, 1.5GHz**.
- 벤치마크(800×600 프레임):
  - yolo26n@640: 약 110ms
  - yolo26s@640: 약 235ms
  - OpenVINO는 이 조건에서 이득이 없었습니다.
- 모델 확인:
  - sauce-hug 번호판 `.pt` 3종: 글자 클래스 75개가 원본 목록과 일치
  - TwinLiteNet `pretrained/best.pth`가 공개되어 있음 (다음 단계용)

## 5. 구현

- `csd` 패키지 16개 모듈을 작성했습니다. 구조는 architech.md를 참고하세요.
- 수정 1: 한 차량의 등화, 주행, 충돌 상태가 서로 중복 억제를 덮어쓰는 문제 → `EventLog` 채널 도입.
- 수정 2: framesize 번호 QXGA=21로 추정했던 값 → 설치된 `sensor.h`에서 확인해 19로 수정.
- 수정 3: 번호판 글자 목록을 추측하지 않도록 원본 `kor_list.txt` 75개를 그대로 반영.

## 6. 테스트

- 1차 실행: 45개 중 3개 실패.
  - OpenCV 5에서 `HoughLinesP` 반환 형태가 `(N,4)`로 바뀜 → `reshape(-1,4)`로 수정.
  - 끼어들기 테스트 설정 오류: 옆 차선 차량을 차선 경계 위에 둠 → 테스트 수정.
- 차선 결함 발견: 좁은 ROI 때문에 차가 치우치면 반대편 차선을 놓침.
  - 전체 폭에서 검색하고 **하단에서 중앙에 가장 가까운 선**을 선택하도록 변경.
  - 옆 차선의 선을 무시하는지 검증하는 테스트를 추가.
- 결과: **48개 모두 통과**.

## 7. 실제 카메라 연동 (19:18~19:27)

- 1차 20초: 55프레임(약 2.7fps). 로그 형식이 정상이었습니다.
  - 주변 상황 로그가 깜빡임 → 평활화 추가.
- 번호판 처리 시간: 첫 글자 인식 1.6초 → 백그라운드 스레드로 분리.
- 음성 실행: Heami로 "차량 신호 감지를 시작합니다"를 확인했습니다.
  - 실내 차선 오검출로 이탈이 깜빡임 → 차선 평활화 추가(1초 유지, 70%).
- 사용자 요청으로 **운전 상황 모니터**를 추가했습니다.
  - 음성 자막 큰 글씨, 신호등, 위험 감지, 앞차·번호판, 차선 그림, 분석 결과 표, 이동 경로 궤적, 이벤트 로그
  - 합성 상태로 렌더링을 검증했고(`docs/images/dashboard_demo.png`), 실제 카메라 스냅샷도 확인했습니다.
- 발견: 실내 영상이 매우 어둡고, 자차 "주행" 오판이 있습니다(HANDOVER K2, K7).

## 8. 인수인계 문서화와 GitHub 푸시

- HANDOVER.md, architech.md, dev_plan.md, research.md, README.md를 작성했습니다.
- `.gitignore`로 `.venv`, `models/`, `logs/`, `scratch/`를 제외했습니다.
- https://github.com/MacTechIN/car_siginal_detector 로 푸시했습니다.

## 9. v0.2 — 녹화, 음성 라벨링, 학습, 차량 시연 준비 (20:50~22:30)

사용자 요청 순서:
1. 녹화 도구
2. 한국 신호등 학습 데이터 수집 방안과 학습 기능
3. 글자 입력 대신 음성으로 라벨 입력
4. 내일 차량에서 직접 시연

- **녹화:** `tools/record.py`와 `csd/recorder.py`를 만들었습니다.
  - 원본 JPEG를 다시 압축하지 않고 저장하며, `timestamps.csv`에 도착 시각을 기록합니다.
  - 20초 녹화: 182프레임, 9.2fps(XGA), 4.2MB.
  - 재생(`FileSource`)이 기록 시각을 재현합니다. 182프레임을 모두 처리했습니다.
  - `cv2.imread`가 한글 경로를 열지 못해 `np.fromfile` + `imdecode`로 바꿨습니다.
- **JPEG 분리 로직 공용화:** `split_jpegs`/`iter_mjpeg`를 만들어 녹화와 실시간 수신이 함께 씁니다.
  - multipart 헤더를 섞어 7바이트씩 잘라 넣는 테스트도 추가했습니다.
- **카메라 펌웨어** (차 안에는 집 Wi-Fi가 없으므로):
  - 알려진 Wi-Fi에 각 12초씩 시도하고, 실패하면 자체 AP `CSD-CAM`을 엽니다. mDNS `esp32cam`도 등록합니다.
  - 업로드하자 `frame buffer malloc failed`가 발생했습니다.
    - 18:35의 정상 바이너리를 다시 올리면 정상이었고, 빌드 옵션과 sdkconfig도 같았습니다.
    - mDNS를 빼도 재현되어, 코드 쪽으로 좁혔습니다.
    - PSRAM 진단 출력(`found=1, free 8MB`)을 넣어 확인했습니다.
    - 원인은 **예제의 `camera_config_t config;` 초기화 누락**이었습니다. `= {}`로 고쳐 해결했습니다.
  - AP 강제 빌드(`build_opt.h`의 `-DFORCE_AP`)로 노트북에서 `CSD-CAM`이 잡히는 것을 확인했습니다(-49dBm).
    - 노트북을 실제로 AP에 연결하는 시험은 작업 세션 인터넷이 끊기므로 하지 않았습니다.
  - 정상 빌드를 다시 올렸습니다. mDNS는 집 중계기 네트워크에서 조회되지 않았습니다.
  - 펌웨어 소스를 `firmware/camera_web_server/`에 복사했습니다(비밀번호 파일 제외).
- **카메라 자동 탐색:** `csd/discover.py`. 설정 주소 → AP → mDNS → 서브넷 순서로 찾습니다.
- **음성 라벨링:** `csd/voice_label.py`와 `csd/labeling.py`.
  - Vosk 한국어 small 모델을 씁니다. 한글 절대 경로에서는 로드에 실패해서 `ascii_model_path`로 우회했습니다.
  - 합성 음성 평가 결과:
    - 자유 인식은 쓸 수 없는 수준이었습니다.
    - 문장 목록 모드는 17/19였고, 일반 대화도 라벨로 강제 인식됐습니다(신뢰도 1.0).
    - 방해 단어, 정확 일치 규칙, 짧은 음절 제거를 적용한 뒤 **21/21 정확, 오인 0/15**가 됐습니다.
  - 자기 음성 차단: TTS 스레드가 기록한 발화 시각을 기준으로 마이크 입력을 버립니다. COM 객체는 다른 스레드에서 호출하지 않습니다.
  - 점멸 라벨은 크롭을 저장하지 않고 이벤트만 기록합니다(꺼진 프레임이 섞이는 오염 방지).
- **학습:**
  - `tools/build_tl_dataset.py`: 라벨 이벤트 단위로 train/val을 나눕니다.
  - `tools/train_tl.py`: YOLO26n-cls, 96px, 색상 보존 증강.
    - Ultralytics 분류 학습의 기본 무작위 크롭 면적은 50~100%라 램프가 잘릴 수 있어, `scale=0.1`(90~100%)로 줄였습니다.
  - 합성 데이터 종단 시험: 5에폭 약 1분, 합성 val 99.1%. 앱의 분류기 로드와 예측까지 확인했습니다.
  - 합성 모델은 `models/`에 설치하지 않았습니다(`scratch/`에만 둠).
- **시연 도구:** `tools/preflight.py`, `demo.bat`, `train.bat`, `--fullscreen`, `DEMO.md`.
  - 점검 결과 **배터리 8%(충전 안 됨)와 마이크 무음(rms 1)** 을 찾아 사용자에게 알렸습니다.
- **테스트:** 79개 모두 통과(약 15초).
  - 원래 5초였는데 루트에서 `pytest`를 돌리면 수집에 93초가 걸렸습니다. `recordings/` 등을 뒤지기 때문이라 `pytest.ini`로 해결했습니다.
- **실제 카메라 종단 실행:** 자동 탐색, 설정, 녹화 154프레임, 분석 72프레임, 음성 라벨 대기까지 정상.

## 10. 차 안 인터넷: 휴대폰 USB 테더링 구성 (2026-09-25)

- 사용자 질문: `CSD-CAM`에 연결하면 인터넷이 끊기는데 USB 케이블로 할 수 없나?
- 두 가지 방법을 제시했습니다.
  1. 인터넷은 휴대폰 USB 테더링으로 받고, Wi-Fi는 카메라용으로 씁니다. 코드 변경 없음.
  2. 카메라 영상을 USB로 받습니다. 펌웨어와 앱 수정이 필요합니다.
- 사용자가 **1번**을 선택했습니다.
- `tools/preflight.py`에 **인터넷 점검**을 추가했습니다.
  - 인터넷 연결 여부, 인터넷이 나가는 경로 어댑터(최소 메트릭 기본 경로), 현재 Wi-Fi SSID를 보여줍니다.
  - 인터넷이 없어도 앱은 오프라인으로 동작하므로 실패(FAIL)가 아니라 주의(WARN)로 처리합니다.
- `DEMO.md`의 한 장 요약과 2-1장에 테더링 절차를 넣었습니다. 인터넷이 안 될 때의 Wi-Fi 메트릭 조정 명령과 원복 명령도 넣었습니다.
- 점검 실행 결과:
  - 전원: 충전 중으로 바뀜.
  - 마이크: 정상(rms 14)으로 바뀜.
  - 인터넷: Wi-Fi(`iptime5G`) 경로로 연결됨.
  - 카메라: 노트북이 카메라가 없는 네트워크에 연결되어 있어 찾지 못함(차 안에서는 `CSD-CAM`으로 찾음).
- 실제 휴대폰 테더링과 `CSD-CAM` 동시 연결은 시험하지 못했습니다.
