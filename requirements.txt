ultralytics>=8.4
opencv-python>=4.10
numpy>=2.0
requests>=2.31
pyyaml>=6.0
pywin32>=306; sys_platform == "win32"
lap>=0.5            # ByteTrack association (installed on demand by ultralytics otherwise)
vosk>=0.3.45        # offline Korean speech recognition for voice labelling
sounddevice>=0.4    # microphone capture
pyserial>=3.5       # camera frames over the USB cable (csd/usb_camera.py)
psutil>=5.9         # battery check in tools/preflight.py
openvino>=2025.0    # optional: model.export(format="openvino")
onnxruntime>=1.18   # optional: ONNX backend
pytest>=8.0
