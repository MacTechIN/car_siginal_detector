#pragma once
// Copy to wifi_secrets.h (git-ignored) and fill in. 2.4 GHz networks only.
#define WIFI_SSID     "home-wifi-name"
#define WIFI_PASSWORD "home-wifi-password"

// Optional second network, e.g. a phone hotspot used in the car:
// #define WIFI_SSID2     "hotspot-name"
// #define WIFI_PASSWORD2 "hotspot-password"

// Optional: access point the camera opens when no known network is found
// (defaults: "CSD-CAM" / "csdcam1234", address 192.168.4.1)
// #define AP_SSID     "CSD-CAM"
// #define AP_PASSWORD "csdcam1234"
