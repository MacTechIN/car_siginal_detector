# DEMO.md — 차량 시연 안내 (2026-09-25)

## 0. 전날 밤 준비 (집에서)

- [ ] **노트북 완전 충전.** 2026-09-24 22시 점검 때 배터리가 8%였습니다.
- [ ] **마이크 확인:** 설정 → 개인 정보 및 보안 → 마이크 → "데스크톱 앱이 마이크에 액세스하도록 허용" 켜기. 마이크 음소거도 해제합니다.
  - 점검 도구에서 `마이크: 동작`이 나와야 합니다(22시에는 rms 1로 거의 무음이었음).
- [ ] 카메라 보드에 새 펌웨어가 올라가 있음 (자체 Wi-Fi 지원, 2026-09-24 21:40 업로드 완료).
- [ ] 휴대폰 핫스팟을 쓰려면: `esp32_cam\firmware\camera_web_server\wifi_secrets.h`에 아래 두 줄을 추가하고 다시 업로드합니다.

  ```c
  #define WIFI_SSID2 "핫스팟이름"
  #define WIFI_PASSWORD2 "비밀번호"
  ```

  핫스팟 2.4GHz 필수. 없으면 아래 **카메라 자체 Wi-Fi**를 씁니다.

## 1. 장비

| 장비 | 비고 |
|---|---|
| ESP32-S3 카메라 보드 | 앞유리 안쪽, 차량 중앙, 수평으로 고정. 렌즈가 도로 정면을 향하게 |
| USB 전원 (차량 USB 또는 시거잭 충전기) | 보드의 **CH340 쪽 USB-C**에 연결 (어느 포트든 전원은 됨) |
| 노트북 + 충전기 (인버터/USB-C PD) | **배터리 모드는 CPU가 1.5GHz로 떨어져 2~3fps밖에 안 나옵니다** |
| (선택) 휴대폰 핫스팟 | 2.4GHz |

## 2. 네트워크 연결 — 카메라가 스스로 결정합니다

카메라 전원을 켜면 아래 순서로 연결을 시도합니다. 각 12초씩입니다.
1. 집 Wi-Fi
2. `WIFI_SSID2` (설정한 경우, 예: 핫스팟)
3. 둘 다 없으면 **자체 Wi-Fi `CSD-CAM`을 엽니다** (비밀번호 `csdcam1234`, 주소 192.168.4.1)

**차 안에서는 보통 3번이 됩니다.** 전원을 켜고 약 30초 뒤, 노트북 Wi-Fi 목록에서 `CSD-CAM`에 연결하세요.
- 이때 노트북은 인터넷이 끊깁니다. 이 앱은 모두 오프라인으로 동작하므로 문제없습니다.
- 앱이 카메라 주소를 자동으로 찾습니다: 설정 주소 → 192.168.4.1 → esp32cam.local → 같은 네트워크 검색.

## 3. 시연 시작

1. `car_siginal_detector` 폴더에서 **`demo.bat` 더블클릭**
2. [1/2] 출발 전 점검 결과를 음성으로 알려줍니다. 전원, 디스크, 모델, 음성, 마이크, 카메라 fps를 확인합니다.
3. [2/2] 모니터가 전체 화면으로 열리고 "차량 신호 감지를 시작합니다"라고 안내합니다.
4. 종료: 모니터 창에서 **q** 또는 **Esc**.
   - 녹화와 라벨은 `recordings\<날짜시각>_demo\`에 저장됩니다.

직접 실행할 때의 명령:

```powershell
.\.venv\Scripts\python.exe tools\preflight.py
.\.venv\Scripts\python.exe -m csd --show --fullscreen --label-voice --name demo
.\.venv\Scripts\python.exe -m csd --show                 # 라벨링·녹화 없이 보기만
```

## 4. 운전 중 음성 라벨링 (손 안 쓰고 학습 데이터 모으기)

신호등이 보일 때 **그 상태를 짧게 말하면** 됩니다.
- 앱이 말을 시작한 순간 전후(-1초 ~ +0.6초)의 신호등 크롭을 저장합니다.
- 저장하면 "빨간불 5장 저장"처럼 음성으로 확인해 줍니다.

| 이렇게 말하기 | 저장되는 라벨 |
|---|---|
| **빨간불** / 적색 | red |
| **노란불** / 황색 | yellow |
| **초록불** / **파란불** / 녹색 | green |
| **좌회전** (화살표만 켜짐) | left |
| **직진 좌회전** / 초록 좌회전 / 파란 좌회전 | green_left |
| **빨간 좌회전** | red_left |
| **황색 점멸** / 노란 점멸 | flashing_yellow (이벤트만 기록, 크롭은 저장 안 함) |
| **적색 점멸** / 빨간 점멸 | flashing_red (이벤트만 기록, 크롭은 저장 안 함) |
| **신호 없음** | off |
| **맞아** | 화면에 표시된 현재 판별 결과를 그대로 정답으로 저장 |
| **취소** | 바로 앞 라벨 삭제 |

- 목록에 있는 표현 **하나만** 말하세요. "빨간불이네"처럼 다른 말이 붙으면 무시됩니다. 대화나 라디오가 잘못 저장되지 않게 하려는 설계입니다.
- 안내 음성이 나오는 동안에는 마이크 입력을 무시합니다(앱이 자기 목소리를 듣지 않도록). 안내가 끝난 뒤 말하세요.
- 모니터 하단 오른쪽에 "음성 라벨 듣는 중 / 들은 말 / 저장된 크롭 수"가 표시됩니다.
- 신호등이 화면에서 검출되지 않은 때 말하면 "신호등이 보이지 않아 영상 시각만 기록했습니다"라고 안내합니다.

## 5. 시연 후: 학습

```powershell
train.bat
```

- 데이터셋을 만들고(`dataset\tl_cls`) 학습합니다(CPU, 수 분). 결과는 `models\tl_cls.pt`에 설치됩니다.
- **다음 실행부터 자동으로 학습 모델을 사용합니다.** 확신이 낮으면 규칙 판별로 돌아갑니다.
- 클래스마다 크롭이 20장 미만이면 경고합니다. 한 클래스에 이벤트 10회 이상(크롭 수십 장)을 권장합니다.
- 추가 데이터(예: AI Hub 신호등 크롭)는 `data\extra_tl\<클래스>\*.jpg`에 넣으면 함께 학습합니다.

## 6. 문제 해결

| 증상 | 조치 |
|---|---|
| "카메라를 찾는 중입니다"가 반복됨 | 카메라 USB 전원 확인 → 30초 대기 → 노트북 Wi-Fi를 `CSD-CAM`에 연결 |
| 화면이 너무 어두움 / 신호등 색이 하얗게 날아감 | `configs\default.yaml`의 `camera.settings.ae_level` 조정 (-1 → 0 또는 -2) |
| fps가 2 이하 | 노트북 충전기 연결, 전원 모드 "최고 성능". 또는 `detector.model`을 `models/yolo26n.pt`로 |
| 음성 라벨이 반응 없음 | 점검 도구의 마이크 항목 확인. 모니터의 "들은 말"에 아무것도 안 나오면 마이크 문제 |
| 모니터 창이 안 닫힘 | 창을 클릭한 뒤 q. 콘솔에서 Ctrl+C |

## 7. 알아둘 한계 (시연 때 설명용)

- 모든 판별은 아직 **실제 도로에서 검증되지 않은 첫 버전**입니다. 내일 녹화가 그 검증 데이터가 됩니다.
- 신호등은 규칙 기반으로 시작합니다. 음성 라벨로 모은 데이터로 학습하면 점점 정확해집니다.
- 자차 속도 정보(GPS/OBD)가 없어서, 앞차의 "정지/출발"은 상대 움직임으로만 추정합니다.
